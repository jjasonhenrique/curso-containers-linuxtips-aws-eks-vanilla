apiVersion: v1
kind: Service
metadata:
  name: {{ include "linuxtips.fullname" . }}
  labels:
    {{- include "linuxtips.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linuxtips.selectorLabels" . | nindent 4 }}
